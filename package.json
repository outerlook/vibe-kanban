{
  "name": "vibe-kanban",
  "version": "0.0.143",
  "private": true,
  "scripts": {
    "server": "cargo run --bin server",
    "lint": "npm run frontend:lint && npm run backend:lint",
    "format": "cargo fmt --all && cd frontend && npm run format",
    "check": "npm run frontend:check && npm run backend:check",
    "dev": "export FRONTEND_PORT=$(node scripts/setup-dev-environment.js frontend) && export BACKEND_PORT=$(node scripts/setup-dev-environment.js backend) && concurrently \"npm run backend:dev:watch\" \"npm run frontend:dev\"",
    "frontend:lint": "cd frontend && npm run lint",
    "frontend:dev": "cd frontend && npm run dev -- --port ${FRONTEND_PORT:-3000} --host",
    "frontend:check": "cd frontend && npm run check",
    "backend:lint": "cargo clippy --workspace --all-targets --all-features -- -D warnings",
    "backend:dev": "BACKEND_PORT=$(node scripts/setup-dev-environment.js backend) npm run backend:dev:watch",
    "backend:check": "cargo check",
    "backend:test": "cargo nextest run --workspace",
    "backend:dev:watch": "DISABLE_WORKTREE_ORPHAN_CLEANUP=1 RUST_LOG=debug cargo watch -w crates -x 'run --bin server'",
    "generate-types": "cargo run --bin generate_types",
    "generate-types:check": "cargo run --bin generate_types -- --check",
    "prepare-db": "node scripts/prepare-db.js",
    "prepare-db:check": "node scripts/prepare-db.js --check",
    "remote:dev": "cd crates/remote && docker compose --env-file .env.remote up --build ; docker compose down -v",
    "remote:prepare-db": "cd crates/remote && bash scripts/prepare-db.sh",
    "remote:prepare-db:check": "cd crates/remote && bash scripts/prepare-db.sh --check",
    "langfuse:up": "docker compose -f docker-compose.langfuse.yml up -d",
    "langfuse:down": "docker compose -f docker-compose.langfuse.yml down",
    "langfuse:logs": "docker compose -f docker-compose.langfuse.yml logs -f",
    "seed-db": "cargo run --bin generate-seed",
    "audit:orchestrator-cutover": "if rg -n --hidden --glob '!node_modules' --glob '!.git' --glob '!target' --glob '!dist' --glob '!build' --glob '!pnpm-lock.yaml' --glob '!Cargo.lock' '\\b\\x6e8n\\b|\\x6e8n-|\\x6e8n\\.example' .; then echo 'Found stale legacy orchestrator references'; exit 1; fi",
    "trigger:install": "cd integrations/trigger-vibe-kanban && bun install --frozen-lockfile",
    "trigger:ci": "npm run trigger:install && npm run trigger:check && npm run trigger:test",
    "trigger:check": "pnpm run generate-types:check && cd integrations/trigger-vibe-kanban && bun run check",
    "trigger:test": "cd integrations/trigger-vibe-kanban && bun test",
    "trigger:dev": "node scripts/trigger-vk.js dev",
    "trigger:mqtt": "node scripts/trigger-vk.js mqtt",
    "trigger:coderabbit": "node scripts/trigger-vk.js coderabbit",
    "trigger:deploy": "node scripts/trigger-vk.js deploy",
    "trigger:push": "node scripts/trigger-vk.js push",
    "trigger:deploy:staging": "node scripts/trigger-vk.js deploy:staging",
    "trigger:deploy:dry-run": "node scripts/trigger-vk.js deploy:dry-run",
    "trigger:stack": "concurrently \"npm run trigger:dev\" \"npm run trigger:mqtt\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "sass-embedded": "^1.97.2",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@10.13.1"
}
